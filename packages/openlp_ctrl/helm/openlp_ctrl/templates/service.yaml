apiVersion: v1
kind: Service
metadata:
  name: {{ include "openlp-ctrl.fullname" . }}
  labels:
    {{- include "openlp-ctrl.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openlp-ctrl.selectorLabels" . | nindent 4 }}
